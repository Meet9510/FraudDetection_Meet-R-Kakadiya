# Makes 'dashboard' a proper Python package so
# 'from dashboard.theme import ...' resolves correctly
# both at runtime (when run from project root) and
# in the IDE (Pylance / pyright static analysis).
